[project]
name = "json-bulk-importer-ankivn"
version = "0.1.0"
description = "🚀 JSON Bulk Importer - from AnkiVN with ❤️"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["json_bulk_importer"]
